# Claude Nook Remote Configuration Template
#
# Copy this file to ~/.config/claude-nook/claude-nook.env
# and fill in your connection details.
#
# Then add to your shell profile (~/.zshrc or ~/.bashrc):
#   if [ -f ~/.config/claude-nook/claude-nook.env ]; then
#       set -a
#       source ~/.config/claude-nook/claude-nook.env
#       set +a
#   fi

# =============================================================================
# CONNECTION MODE
# =============================================================================
# Options:
#   auto   - Try Unix socket first, fall back to TCP (default for local dev)
#   socket - Unix socket only (local machine only)
#   tcp    - TCP only (remote machines)
CLAUDE_NOOK_MODE=tcp

# =============================================================================
# TCP CONNECTION SETTINGS
# =============================================================================
# The hostname or IP of your Mac running Claude Nook
# Examples:
#   - 192.168.1.100 (local network IP)
#   - mymac.local (Bonjour name)
#   - my-mac (Tailscale hostname)
#   - 127.0.0.1 (if using SSH tunnel)
CLAUDE_NOOK_HOST=<your-mac-ip-or-hostname>

# TCP port (must match Claude Nook app settings)
CLAUDE_NOOK_PORT=4851

# Authentication token (get this from Claude Nook app settings)
# This is a 64-character hex string
CLAUDE_NOOK_TOKEN=<your-64-char-token>

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Timeout in seconds for permission decisions (default: 300 = 5 minutes)
# CLAUDE_NOOK_TIMEOUT=300

# Enable debug output to stderr (useful for troubleshooting)
# CLAUDE_NOOK_DEBUG=1
